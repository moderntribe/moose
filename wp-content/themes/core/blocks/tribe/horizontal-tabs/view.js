import tabs from 'utils/tabs';

/**
 * @function init
 *
 * @description kick off the functionality
 */
const init = () => {
	tabs( document.querySelectorAll( '.wp-block-tribe-horizontal-tabs' ) );
};

init();
