/**
 * Some code here!
 */

/**
 * TODO: Move this to a core/image specific partial when we figure out that architecture.
 */
import( '../core-image-lazyload' );

console.info( 'tribe-admin js loaded' );
